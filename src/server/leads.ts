// Lead capture + CRM service layer (prj.md Section 3). Public capture is open;
// status updates and follow-up notes require an authenticated Owner/Admin.
//
// TDD placeholder: add leads.test.ts (capture validation, status pipeline
// new -> contacted -> negotiating -> converted/lost, follow-up notes) first.

export {};
